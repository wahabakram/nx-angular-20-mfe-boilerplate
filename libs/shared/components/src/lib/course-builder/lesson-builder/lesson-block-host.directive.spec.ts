import { LessonBlockHostDirective } from './lesson-block-host.directive';

describe('LessonBlockHostDirective', () => {
  it('should create an instance', () => {
    const directive = new LessonBlockHostDirective();
    expect(directive).toBeTruthy();
  });
});
