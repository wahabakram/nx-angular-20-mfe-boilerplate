import { ContentChangesObserverDirective } from './content-changes-observer.directive';

describe('ContentChangesObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentChangesObserverDirective();
    expect(directive).toBeTruthy();
  });
});
