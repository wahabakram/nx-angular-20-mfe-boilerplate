import { ContentObserverDirective } from './content-observer.directive';

describe('ContentObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentObserverDirective();
    expect(directive).toBeTruthy();
  });
});
