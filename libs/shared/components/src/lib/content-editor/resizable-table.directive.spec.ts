import { ResizableTableDirective } from './resizable-table.directive';

describe('ResizableTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableTableDirective();
    expect(directive).toBeTruthy();
  });
});
