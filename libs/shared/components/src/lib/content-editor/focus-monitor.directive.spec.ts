import { FocusMonitorDirective } from './focus-monitor.directive';

describe('FocusMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusMonitorDirective();
    expect(directive).toBeTruthy();
  });
});
