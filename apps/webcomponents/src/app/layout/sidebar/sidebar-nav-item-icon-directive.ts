
import { Directive, inject, TemplateRef } from '@angular/core';

@Directive({
  selector: '[mfcSidebarNavItemIcon]'
})
export class SidebarNavItemIconDirective {
  // readonly templateRef = inject(TemplateRef);
}
